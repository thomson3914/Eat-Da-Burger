<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center" id="top-bar">
      <h1>EAT DA BURGER!</h1>
    </div>
    <div class="col-xs-6 text-center" class="task">
      <div class="row">
        {{#each burger_data}} {{#unless this.devoured}}
        <div class="col-xs-9 text-center">
          <pre>{{this.id}}. {{this.burger_name}}</pre>
        </div>
        <div class="col-xs-3 text-center">
          <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
            <input type="hidden" name="burger_id" type="text" value={{this.id}}>
            <br>
            <button type="submit" class="btn btn-default">DEVOUR IT!</button>
          </form>
        </div>
        {{/unless}} {{/each}}
      </div>
    </div>

    <div class="col-xs-6 text-center" class="task">
      {{#each burger_data}} {{#if this.devoured}}
      <input class="form-control" type="text" placeholder="{{this.id}}. {{this.burger_name}}" readonly> {{/if}} {{/each}}
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center enter_section">
      <form action="/burgers/create" method="POST">
        <input type="text" name="burger_name" class="form-control" value="" id="enter_text">
        <button type="submit" class="btn btn-primary" id="text-enter-button">SUBMIT</button>
      </form>
      <p>Enter in a burger you would like to eat!</p>
    </div>
  </div>
</div>

{{!--
<div class="container">
  <div class="jumbotron">
    <h1 class="text-center">Burger, I'd Like to eat...</h1>
    <form class="text-center" action="/" method="POST">
      <div class="form-group">
        <input type="text" class="form-control" id="burger_id" name="name" placeholder="Nom Nom">
      </div>
      <button class="btn btn-default btn-danger" type="submit">Make Da Burger</button>

    </form>
  </div>
  <div class="row">
    <div class="col-lg-4 col-md-4 col-sm-4">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title text-center">Please</h3>
        </div>
        <div class="panel-body">
          <ul class="list-group">
            {{#each burger_data}} {{#unless this.devoured}}
            <li class="list-group-item">
              <p class="burger_name">{{this.name}}</p>
              <form class="pull-right" action="/burgers/update?_method=PUT" method="POST">
                <input type="hidden" name="devoured" value="true">
                <button class="btn btn-default btn-danger" type="submit">Eat!</button>
              </form>
            </li>
            {{/unless}} {{/each}}
          </ul>
        </div>
      </div>
    </div>
    <div class="col-lg-4 col-md-4 col-sm-4">
      <div class="hero panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title text-center">Eat Me</h3>
        </div>
        <div class="panel-body">
          <img class="img-responsive" src="../assets/img/cat-burger.jpg" />
        </div>
      </div>
    </div>
    <div class="col-lg-4 col-md-4 col-sm-4">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title text-center">Thank You</h3>
        </div>
        <div class="panel-body">
          <ul class="list-group">
            {{#each burger_data}} {{#if this.devoured}}
            <li class="list-group-item">
              <p class="burger_name">{{this.name}}</p>
              <form class="pull-right" action="/{{this.id}}?_method=DELETE" method="POST">
                <button class="btn btn-default btn-danger" type="submit">Poop!</button>
              </form>
            </li>
            {{/if}} {{/each}}
          </ul>
        </div>
      </div>
    </div>
  </div>


</div> --}}